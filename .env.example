# Inlined into the CRA build. Public anon key only — never the service role key.
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=

# Server-only. Used by api/claude.js for the classic invoice/import tools.
ANTHROPIC_API_KEY=
